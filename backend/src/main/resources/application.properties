spring.application.name=backend

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:${DB_PORT:5433}/${DB_NAME:almaeng}
spring.datasource.username=${DB_USERNAME:almaeng}
spring.datasource.password=${DB_PASSWORD}

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# Redis Configuration
spring.data.redis.host=localhost
spring.data.redis.port=${REDIS_PORT:6379}

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=true

# Mattermost Webhook ??
notification.mattermost.enabled=true
notification.mattermost.webhook-url=https://meeting.ssafy.com/hooks/4ahsa54oh3geiboxzc9yazwh8o
notification.mattermost.channel=
notification.mattermost.pretext=@here ? ?? ?? ?? ?
notification.mattermost.color=#ff5d52
notification.mattermost.author-name=AL-MAENG Error Bot
notification.mattermost.author-icon=
notification.mattermost.title=??? ??? ?? ??